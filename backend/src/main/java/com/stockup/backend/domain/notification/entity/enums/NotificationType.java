package com.stockup.backend.domain.notification.entity.enums;

public enum NotificationType {
    BASKET_BROADCASTED,
    OFFER_SUBMITTED,
    RESERVATION_CREATED,
    RESERVATION_ACTIVATED,
    RESERVATION_CANCELLED,
    RESERVATION_COMPLETED,
    RESERVATION_EXPIRED
}
